apiVersion: v1
kind: Service
metadata:
  name: vprocache01
  labels:
    app: vpromc
spec:
  selector:
    app: vpromc
  ports:
    - name: vpromc-port
      port: 11211
      targetPort: vpromc-port
      protocol: TCP
  type: ClusterIP